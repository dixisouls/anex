redis>=5.0
pydantic>=2.6
numpy>=1.26

# Persistence (async)
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
alembic>=1.13

# Only needed when EMBED_BACKEND=vertex. Not required for local development.
# google-cloud-aiplatform>=1.60