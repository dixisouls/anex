# Agent Bazaar backend dependencies.
# Versions verified latest on 2026-06-06 (see docs/cloud-architecture.md §13 for sources).
# Pins use lower bounds at the verified-latest so a fresh install is reproducible-ish
# while still allowing patch updates; tighten to == if you want a hard lock.

# --- Core / hot path ---
redis>=8.0,<9            # 8.0.0 (2026-05-28). Async client + native vector KNN (FLAT). Memorystore 7.2+ compatible.
pydantic>=2.13.4,<3      # 2.13.4 (2026-05-06)
numpy>=2.4.6,<2.5        # 2.4.6 (2026-05-18)

# --- Persistence (async) ---
sqlalchemy[asyncio]>=2.0.50,<2.1   # 2.0.50 (2026-05-24)
asyncpg>=0.31                       # 0.31.0 (2025-11-24)
alembic>=1.18.4                     # 1.18.4 (2026-02-10)

# --- Models (cloud only; no offline model or embedding calls) ---
google-genai>=2.8,<3     # 2.8.0 (2026-06-03). Gemini Enterprise Agent Platform: chat + embeddings (gemini-embedding-001).
openai>=2.41,<3          # 2.41.0 (2026-06-03). Worker variety + simulation (Responses API).

# --- API + broker HTTP + SSE ---
fastapi>=0.136.3         # 0.136.3 (2026-05-23)
uvicorn[standard]>=0.49  # 0.49.0 (2026-06-03)
httpx>=0.28.1,<1         # 0.28.1 (2024-12-06). Broker->agent HTTP (local Queue adapter) + sim API calls.
sse-starlette>=3.4.4     # 3.4.4 (2026-05-12). EventSourceResponse for GET /feed.

# --- Observability (mandatory) ---
weave>=0.52.42           # 0.52.42 (2026-06-02). Tracing for every LLM + exchange op.

# --- Cloud adapters (only needed when RUNTIME_ENV=gcp; safe to install everywhere) ---
google-cloud-tasks>=2.22            # 2.22.0 (2026-03-26). Queue port: hire dispatch (HTTP push + OIDC).
google-cloud-pubsub>=2.38           # 2.38.0 (2026-05-07). EventBus port: cloud event fan-out to SSE.
google-cloud-secret-manager>=2.28   # 2.28.0 (2026-05-07). Read secrets at startup if not using --set-secrets.
cloud-sql-python-connector[asyncpg]>=1.20.3  # 1.20.3 (2026-05-27). Cloud SQL private-IP connectivity (lazy refresh).

# --- Dev / test ---
python-dotenv>=1.0       # load .env for local runs (backend/config.py)
pytest>=8.4              # required by pytest-asyncio 1.4.0
pytest-asyncio>=1.4      # 1.4.0 (2026-05-26)
